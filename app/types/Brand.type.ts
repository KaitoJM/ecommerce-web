export interface Brand {
  id: string;
  name: string;
  image: string;
  created_at: string;
}

export interface BrandListItem {
  id: string;
  name: string;
  image: string;
  created_at: string;
}
